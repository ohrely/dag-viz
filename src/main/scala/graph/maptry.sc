val nodeCol:Map[String,Int] = Map()

// Set of all node names
// Set of all dests
// If a node is not a destination, add to Map with current column #, remove from nodes list,
// and remove edges with it as a source
// Increase column #
// Repeat (recursion)

// Inputs: node list, edges list, column number (start at 0)
// Output: Map(?)
// Exit: empty nodes list

def columnMapper(nodes: List[Int], edges: List[Edge], col: Int) {
  if (nodes.isEmpty: Boolean){
//    exit condition
  }
  
}
